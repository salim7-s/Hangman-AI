{
  "name": "hangman-ai",
  "description": "Full-stack Hangman game with React, Node.js, multiplayer rooms, and AI guessing modes.",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently \"npm --prefix backend run start\" \"npm --prefix frontend run dev -- --host 0.0.0.0\"",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix backend run test",
    "benchmark:ai": "npm --prefix backend run benchmark:ai",
    "words:build": "npm --prefix backend run words:build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
